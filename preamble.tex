\documentclass{book}

\usepackage[margin=0.5in, 
			paperwidth=5.5in, 
			paperheight=8.5in
		   ]{geometry}
\setlength{\parskip}{6pt}
% Control the space between columns of the table
\setlength{\tabcolsep}{2pt}

\usepackage{glossaries}

% Set up generic titling
\renewcommand\title{Mahjong Rules}
\newcommand\subtitle{Basic Ari Ari Rules}
\renewcommand\author{Matthew Rappaport}